//+build ignore
// This package contains usefull functions used for initializing the 
// platform.Platform, permitting to use GLFW and OpenGL and the entirity of the
// platform package and the gfx package.  
// It provides also a default allocator that should be used over the default
// allocator.
package vx_lib_common